// Copyright 2019 <mmeshcher>
//
//  main.cpp
//  JsonParser
//

#include <iostream>
#include "json.h"
#include <string>

using namespace std;
int main() {
    /*std::cout << "In\n";
    Json object = Json::parseFile("data.json");
    std::cout << "out\n";
    auto it1 = object["lastname"];

//    std::any pr = std::any(make_pair(std::string("use"), 12));
//    std::any& it = pr;
//    auto it3 = std::any_cast<std::pair<std::string, int>>(it);

    auto it2 = std::any_cast<std::string>(object["lastname"]);
    auto it3 = std::any_cast<bool>(object["islegal"]);
    auto it4 = std::any_cast<int>(object["age"]);

    auto marks = std::any_cast<Json>(object["marks"]);

    auto it5 = std::any_cast<int>(marks[0]);
    auto it6 = std::any_cast<int>(marks[1]);

    auto address = std::any_cast<Json>(object["address"]);
    auto it7 = std::any_cast<std::string>(address["city"]);
    auto it8 = std::any_cast<std::string>(address["street"]);
//    std::cout << it.c_str() << "\n";
    // ifstream in("data.json");
    // string str = "";
    // string tmp;
    // while (getline(in, tmp)) str += tmp;
    // JSONValue v = Parser::parse(str);

    // cout << v.to_string() << endl;

    // // you can use jute::parser::parse_file("data.json")

    // cout << " ------ " << endl;
    // cout << v["examples"][0]["attr"][0]["m_value"].as_string() << endl;
    // if (v["examples"][1]["mixed"][5][1][1].as_bool()) {
    //     cout << v["examples"][1]["pie"].as_double() << endl;
    //     cout << v["examples"][2].to_string() << endl;
    // }

    // cout << v["examples"].to_string() << endl;
*/
    return 0;
}
